### a)
- допустим, что оба потока выполняют критическую секцию
- тогда `B.2` должно выполниться при `A_flag = false`. Возмонжо 2 варианта:  `B.2` после `A.5` или `B.2` до `A.1`
- Если `B.2` после `A.5`, то `A.4` до `A.5` до `B.2` до `B.6`. Значит критическая секция будет выполняться неодновременно. Сначала в `A` потом в `B`
- Остается вариант `B.2` до `A.1`, тогда `B.1` до `B.2` до `A.1`. Тк `B.2` до `A.1`, а изначально `A_flag = false`, то `B.3`, `B.4`, `B.5` не будут выполнены
- Чтобы `A` дошел до `A.4` нужно чтобы `A.2` при `B_flag = false`, учитывая прошлый пункт `A.2` должен быть после `B.7`
- Получили `B.1` до `B.2` до `B.6` до `B.7` до `A.2` до `A.4`. Значит критическая секция будет выполняться неодновременно. Сначала в `B` потом в `A`
- Ни в одном из вариантов предположение не выполняется, чтд

### b)
- допуcтим наступил момент времени `t` при кототором произошел дедлок.
- В потоке `A` дедлок мог произойти только в цикле `A.2`, однако `A.1` до `A.2`. Значит `A_flag` перманентно стал равен `true`
- Тк `A_flag = true`, то поток `B` зашел в цикл `B.2`, выполнил `B.3` и завис в `B.4`.
- При этом `B.3` выполнилось и `B_flag` перманентно стал равен `false`
- Рано или поздно проверка `A.2` произойдет после `B.3` и `A` выйдет из дедлока и дойдет до `A.5`, затем и `B` выйдет из дедлока